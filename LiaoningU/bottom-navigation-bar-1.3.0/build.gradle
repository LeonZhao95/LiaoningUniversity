configurations.maybeCreate("default")
artifacts.add("default", file('bottom-navigation-bar-1.3.0.aar'))
repositories {
    flatDir {
        dirs 'libs' //this way we can find the .aar file in libs folder
    }
}